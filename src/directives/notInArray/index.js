/**
 * @ngdoc directive
 * @name directive:notInArray
 * @description
 *
 * Directive for validating a ng-model value
 */
function keybind() {
    return function(scope, element, attrs) {
        // TODO validation based on provided array and contains
    };
}
keybind.$inject = [];

export default keybind;
